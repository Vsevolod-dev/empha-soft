import React, {FC} from 'react';

const Header:FC = () => {
    return (
        <header className={'header'}>
            <div className="container">
                <h1>User list</h1>
            </div>
        </header>
    );
};

export default Header;
